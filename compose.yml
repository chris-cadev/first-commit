services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: first-commit-app:latest
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - BITBUCKET_SERVER_HOSTS=${BITBUCKET_SERVER_HOSTS:-}
